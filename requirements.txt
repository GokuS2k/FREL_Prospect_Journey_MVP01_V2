# FREL Prospect Journey MVP01 — Python dependencies
# Install with: pip install -r requirements.txt

# Snowflake
snowflake-connector-python==3.12.3

# LangChain + LangGraph
# Using ~= (compatible release) so pip can resolve inter-package sub-versions
# while staying within the same major.minor line
langchain~=0.3.25
langchain-core~=0.3.58
langchain-openai~=0.3.16
langgraph~=0.3.21

# OpenAI SDK
openai~=1.75.0

# Data handling
pandas==2.2.3
tabulate==0.9.0

# YAML parsing
pyyaml==6.0.2

# Streamlit UI (1.37+ required for st.audio_input voice recording widget)
streamlit>=1.37.0

# Charts
plotly==5.24.1

# FastAPI backend (replaces Streamlit for React UI)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
# kaleido is used only for chart→PNG export in email attachments.
# It is not installable on Linux/Python 3.11 (Render runtime), so it is
# excluded here. email_sender.py has a built-in graceful fallback when absent.
# Install locally on Windows only: pip install kaleido==0.2.1
