# FREL Prospect Journey MVP01 — Python dependencies
# Install with: pip install -r requirements.txt

# Snowflake
snowflake-connector-python==3.12.3

# LangChain + LangGraph (pinned to avoid breaking API changes)
langchain==0.3.25
langchain-core==0.3.55
langchain-openai==0.3.16
langgraph==0.3.21

# OpenAI SDK
openai==1.75.0

# Data handling
pandas==2.2.3
tabulate==0.9.0

# YAML parsing
pyyaml==6.0.2

# Streamlit UI (1.37+ required for st.audio_input voice recording widget)
streamlit>=1.37.0

# Charts
plotly==5.24.1
kaleido==0.2.1

# Email (stdlib smtplib used directly — no extra package needed)
